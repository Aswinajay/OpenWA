# =====================================================================
# OpenWA - Environment Configuration for Render Free Tier (512 MB RAM)
# =====================================================================
# Copy these environment variables to your Render Web Service dashboard
# under "Environment" -> "Environment Variables", or deploy via render.yaml.
# =====================================================================

# Server & Runtime
NODE_ENV=production
NODE_OPTIONS=--max-old-space-size=384
HOST=0.0.0.0
PORT=10000

# WhatsApp Engine (MANDATORY FOR RENDER FREE 512MB RAM)
# 'baileys' uses pure WebSocket without headless Chrome/Puppeteer.
# DO NOT use 'whatsapp-web.js' on free tier as Chrome exceeds 512MB RAM.
ENGINE_TYPE=baileys
SESSION_DATA_PATH=./data/sessions
AUTO_START_SESSIONS=true
PUPPETEER_SKIP_DOWNLOAD=true

# Database (Lightweight SQLite, no external server needed)
DATABASE_TYPE=sqlite
DATABASE_NAME=./data/openwa.sqlite
DATABASE_SYNCHRONIZE=false
DATABASE_LOGGING=false

# If using external PostgreSQL (e.g., Supabase / Neon / Render Postgres):
# DATABASE_TYPE=postgres
# DATABASE_HOST=your-postgres-host
# DATABASE_PORT=5432
# DATABASE_USERNAME=postgres
# DATABASE_PASSWORD=your-secure-password
# DATABASE_NAME=openwa
# DATABASE_SSL=true

# Security (Master Admin API Key - must be 32+ characters in production)
# In Render, you can set this directly or let render.yaml generate it.
# API_MASTER_KEY=owa_k1_your_32_character_long_secret_key_here

# Turn off memory-heavy optional components
QUEUE_ENABLED=false
REDIS_ENABLED=false
CACHE_ENABLED=false
SEARCH_ENABLED=false
MCP_ENABLED=false

# Dashboard & Docs
SERVE_DASHBOARD=true
ENABLE_SWAGGER=false

# Storage
STORAGE_TYPE=local
STORAGE_LOCAL_PATH=./data/media

# Inbound media memory limits (protects 512MB RAM from media burst spikes)
INBOUND_MEDIA_CONCURRENCY=2
BODY_SIZE_LIMIT=15mb
